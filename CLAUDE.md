# Repository Instructions

## Git Workflow

- Do not create or use git worktrees for this repository.
- When isolation is needed, create a new git branch in the current workspace instead.
- Keep all work in the main repository directory unless the user explicitly asks for a different workflow.
